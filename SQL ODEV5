SELECT title FROM film
WHERE title LIKE '%n'
ORDER BY length ASC
LIMIT 5;                1.)

SELECT title FROM film
WHERE title LIKE '%n'
ORDER BY length DESC
OFFSET 5
LIMIT 5;                 2.)

SELECT * FROM customer
WHERE customer_id = 1
ORDER BY last_name DESC
LIMIT 4;                 3.)
